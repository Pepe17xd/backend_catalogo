services:
  catalog-service:
    image: villabos/catalog-service:v1
    container_name: catalog_api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8081:8081"

networks:
  default:
    name: proycloud_net
