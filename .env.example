# Copy this file to .env before starting the project.
# These values are for local development only; use strong, unique secrets elsewhere.
DB_NAME=cinema_catalog
DB_USER=postgres
DB_PASSWORD=change_me_local
DB_URL=jdbc:postgresql://localhost:5433/cinema_catalog
